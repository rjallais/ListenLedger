# name: oci-build
#
# on:
#   repository_dispatch:
#     types:
#       - forgejo-merge
#   workflow_dispatch:
#     inputs:
#       source_sha:
#         description: "Forgejo commit SHA to build"
#         required: false
#         type: string
#
# permissions:
#   contents: read
#   packages: write
#   id-token: write
#
# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Resolve source sha
#         id: source
#         run: |
#           SHA="${{ github.event.client_payload.sha }}"
#           if [ -z "${SHA}" ]; then
#             SHA="${{ github.event.inputs.source_sha }}"
#           fi
#           if [ -z "${SHA}" ]; then
#             SHA="${{ github.sha }}"
#           fi
#           echo "sha=${SHA}" >> "${GITHUB_OUTPUT}"
#
#       - name: Checkout source
#         uses: actions/checkout@v4
#         with:
#           ref: ${{ steps.source.outputs.sha }}
#           fetch-depth: 0
#
#       - name: Detect Dockerfile
#         id: dockerfile
#         run: |
#           if [ -f Dockerfile ]; then
#             echo "exists=true" >> "${GITHUB_OUTPUT}"
#           else
#             echo "exists=false" >> "${GITHUB_OUTPUT}"
#           fi
#
#       - name: Warn when Dockerfile missing
#         if: steps.dockerfile.outputs.exists == 'false'
#         run: echo "::warning::No Dockerfile found at repository root; skipping OCI build."
#
#       - name: Login to GHCR
#         if: steps.dockerfile.outputs.exists == 'true'
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#
#       - name: Build and push image
#         if: steps.dockerfile.outputs.exists == 'true'
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           tags: |
#             ghcr.io/${{ github.repository }}:${{ steps.source.outputs.sha }}
#             ghcr.io/${{ github.repository }}:main
